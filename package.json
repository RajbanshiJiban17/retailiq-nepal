{
  "name": "retailiq-nepal",
  "version": "1.0.0",
  "private": true,
  "description": "RetailIQ Nepal Root Orchestration",
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build",
    "start": "npm --prefix frontend start"
  }
}
